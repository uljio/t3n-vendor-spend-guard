# Upstream reference
Official walkthrough contract (optional sibling clone):

```bash
git clone https://github.com/Terminal-3/z-tenant-flight.git z-tenant-flight-ref
```

VendorSpendGuard is a separate crate under `vendor-spend-guard/` — do not nest it inside the Node app.
